interface Theme {
  atom: {
    margin: string
    radius: string
    size: string
  },
  box: {
    shadow: {
      bottom: string
      middle: string
      top: string
    }
  },
  brand: {
    background: string
    color: string
    shade: string
    spec: {
      blue: string
      green: string
      red: string
    }
  },
  input: {
    shadow: {
      active: string
      hover: string
    }
  }
}